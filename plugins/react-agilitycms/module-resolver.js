import React, { Component } from 'react';

export default class ModuleResolver extends Component {
    render() {
        
        // const moduleData = this.props.modules.find(mod => {
        //     return mod.item.properties.referenceName === this.props.referenceName
        // })

        // console.log('moduleData', moduleData)
        // const childrenWithProps = React.cloneElement(this.props.children, moduleData)

        return (<h1>ModuleResolver</h1>);
    }
}



